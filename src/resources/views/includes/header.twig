<header class="header">
    <div class="container">
        <div class="topbar">
            <a href="{{ base_url() }}/" class="site-logo"><img src="{{ base_url() }}/img/misc/logo.png" alt="Invision Hair Afghanistan"></a>
            <button id="toggle" class="toggle">
                <i class="toggle__bar"></i>
                <i class="toggle__bar"></i>
                <i class="toggle__bar"></i>
            </button>
        </div>
        <div id="navbar" class="navbar">
            <a href="{{ base_url() }}/" class="navbar__link {{ uri == '/' ? 'navbar__link--hover':''}}">Home</a>
            <a href="{{ base_url() }}/services" class="navbar__link {{ uri == 'services' ? 'navbar__link--hover':''}}">Services</a>
            <a href="{{ base_url() }}/products" class="navbar__link {{ uri == 'products' ? 'navbar__link--hover':''}}">Products</a>
            <a href="{{ base_url() }}/gallery" class="navbar__link {{ uri == 'gallery' ? 'navbar__link--hover':''}}">Gallery</a>
            <a href="{{ base_url() }}/hairreplacement" class="navbar__link {{ uri == 'hairreplacement' ? 'navbar__link--hover':''}}">Hair Replacement</a>
            <a href="{{ base_url() }}/about" class="navbar__link {{ uri == 'about' ? 'navbar__link--hover':''}}">About</a>
            <a href="{{ base_url() }}/contact" class="navbar__link {{ uri == 'contact' ? 'navbar__link--hover':''}}">Contact</a>
            <a href="{{ base_url() }}/cart" class="navbar__link {{ uri == 'cart' ? 'navbar__link--hover':''}}">Cart <i class="fas fa-shopping-cart fa-lg"></i></a>
        </div>
    </div>
</header>
