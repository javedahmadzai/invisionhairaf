{% extends "layouts/master.twig" %}

{% block content %}
    <div class="container">
       <div class="panel">
            <div class="panel__heading">
                Webmaster Area - {% block admin_heading %}{% endblock %}
                <a href="{{ base_url() }}/admin/logout" class="panel__logout">Logout</a>
            </div>
            <div class="panel__left">
                <a href="{{ base_url() }}/admin">Home</a>
                <a href="{{ base_url() }}/admin/gallery">Gallery</a>
                <a href="{{ base_url() }}/admin/emails">Emails</a>
                <a href="{{ base_url() }}/admin/products">Products</a>
                <a href="{{ base_url() }}/admin/slider">Slider</a>
            </div>
            <div class="panel__right">
                {% block admin_content %}{% endblock %}
            </div>
        </div>
    </div>
{% endblock %}
